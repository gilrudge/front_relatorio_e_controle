import { createTheme } from '@mui/material/styles';

const newTheme = createTheme({
  palette: {
    primary: {
      main: '#CC0000',
    },
    secondary: {
      main: '#000000',
    },
  },
});

export default newTheme;